# Medigrand local development environment
# Copy to `.env` and adjust if needed.

# Development database
DB_USER=postgres
DB_PASS=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=medigrand_dev

# Test database (optional overrides)
TEST_DB_USER=postgres
TEST_DB_PASS=postgres
TEST_DB_HOST=localhost
TEST_DB_PORT=5432
TEST_DB_NAME=medigrand_test
